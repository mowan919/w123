# FE-07 字典与系统参数

## 1. 字典
支持字典类型列表、创建、编辑、删除、字典项管理、排序、启用/禁用、默认项。

## 2. 系统参数
与 Dictionary 分离。包括 Session Timeout、Login Max Attempts、Password Min Length、MFA Default Policy 等，具体以 API Contract 为准。

## 3. 字典使用
业务页面通过统一字典 API/Store 获取，不在多个页面硬编码相同枚举。
